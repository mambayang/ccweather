<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ccweather.app"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="22" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.ChooseAreaActivity"
            android:label="@string/title_activity_choose_area" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.ccweather.app.activity.WeatherActivity"
            android:label="@string/title_activity_weather" >
        </activity>

        <service android:name="com.ccweather.app.service.AutoUpdateService" >
        </service>

        <receiver android:name="com.ccweather.app.receiver.AutoUpdateReceiver" >
        </receiver>
    </application>

</manifest>